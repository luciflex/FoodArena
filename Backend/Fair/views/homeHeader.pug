header
    nav
        ul.nav-ul
            li.nav-list
                a.nav-links(href='') Features
            li.nav-list
                a.nav-links(href='') Details
            li.nav-list
                a.nav-links(href='/plans') Plans
            li.nav-list
                a.nav-links(href='') Reviews
            if user
                li.nav-list
                    a.nav-links(href='/profilePage') #{user.name}
                li.nav-list
                    a.nav-links(href='javascript:;').logout LOGOUT 
            else
                li.nav-list
                    a.nav-links(href='/login') Sign-Up
    .showcase
        h1.main-heading
            | Meal Plans
            br
            | For 
            span#change-word
        .main-buttons
            a.heading-buttons.btn-full(href='') Book Now
            a.heading-buttons.btn-empty(href='') Show Me More
